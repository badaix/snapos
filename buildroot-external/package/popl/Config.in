config BR2_PACKAGE_POPL
	bool "popl"
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Header-only C++ program options parser library

	  https://github.com/badaix/popl

comment "popl needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
