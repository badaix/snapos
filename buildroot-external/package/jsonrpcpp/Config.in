config BR2_PACKAGE_JSONRPCPP
	bool "jsonrpc++"
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  C++ JSON-RPC 2.0 library

	  https://github.com/badaix/popl

comment "jsonrpc++ needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
